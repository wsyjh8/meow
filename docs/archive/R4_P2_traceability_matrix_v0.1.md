# P2 Traceability Matrix

**Date**: 2026-04-03
**Purpose**: Map P2 scope items to implementation artifacts, test evidence, and current status.

---

| P2 Scope Item | Backend / API Artifact | Flutter / UI Artifact | Test Evidence | Status | Notes |
|---|---|---|---|---|---|
| **1. Meow Home (喵喵主页)** | `SecondarySummaryController` (`GET /me/secondary-summary`) | `MeowHomePage` at `/meow-home`; `AppRouter.meowHome` | e2e: secondary summary shape tests (4+); Flutter: `meow_home_page_test.dart` (20+ widget tests) | **Implemented** | Cat profile, resources, companion copy, equipped display, feed button, customize entry |
| **2. Reward visibility** | `BalanceSnapshot` in `dev-store.ts`; `getBalanceSnapshot()` computes coins/fish_treats/exp from `RewardLedgerItem`; `SettlementsController` (`POST /settlements/learning-rounds`, `GET /settlements/:id`) | `SecondarySummary` model with coins/fishTreats/exp; Meow Home resource tiles | e2e: 4 secondary-summary tests, 4 settlement tests | **Implemented** | Balance = earned - feed_consumed - coins_spent |
| **3. Basic feed (喂猫)** | `FeedController` (`POST /me/feed`); `feedCat()` in DevStore; `FeedRecord` type; anti-spam (3/day); idempotency | `ApiClient.feedCat()`; `FeedResponse`/`FeedResult` models; feed button in MeowHomePage with loading/success/insufficient/error states | e2e: 5 feed tests; Flutter: 3 feed widget tests | **Implemented** | +4 mood, +2 exp, +1 bond (first 3/day); fish_treat deduction via backend truth |
| **4. Cat growth (EXP/Level)** | `LEVEL_THRESHOLDS` (Lv1-10); `computeLevelFromExp()`; `getTotalExp()`; `growth_feedback` in feed response | `GrowthFeedback` model; level-up `AlertDialog` in MeowHomePage | unit: 11 level tests in `level.spec.ts`; e2e: 4 growth_feedback tests; Flutter: 2 level-up dialog tests | **Implemented** | Level from total exp (reward + feed); caps at Lv10; level-up feedback on feed |
| **5. Basic outfit/decoration** | `CatalogItem` type (5 dev items); `ShopController` (`GET /shop/catalog`, `POST /shop/purchases`); `InventoryController` (`GET /me/inventory`); `EquipmentController` (`GET /me/equipment`, `POST equip`, `POST unequip`); `OwnedItem`, `EquippedSnapshot` types; `purchaseItem()`, `equipItem()`, `unequipItem()` in DevStore | `CatalogResponse`, `InventoryStateData`, `PurchaseResponse`, `EquipmentResponse`, `EquipResponse` models; `/customize` (`CustomizePage`) with buy/equip/equipped three-state; Meow Home equipped card via `equipped_preview` | e2e: 10 purchase + 8 equipment tests; Flutter: 4 model parse + 2 equipped display tests | **Implemented** | Slot-based equip; same-slot replacement; 4 purchase error codes; idempotent |
| **6. Companion copy** | `CompanionResponse` type; `getCompanionResponse()` in DevStore; generates daily_greeting (3 variants), post_learning_response (3 variants + null), streak_node_response (4 nodes + null) | `CompanionResponseData` model; `_buildCompanionCopySection()` in MeowHomePage | e2e: 6 companion response tests; Flutter: 5 companion copy widget tests | **Implemented** | Fact-based (check-in, learning_day, daily_goal_status, session_valid, streak); warm tone; null-safe |
| **7. Check-in/Session visible in secondary** | `has_checked_in_today`, `learning_day_today`, `session_valid_today`, `current_streak` in TodayState; these feed `getCompanionResponse()` | Companion copy section in MeowHomePage changes greeting/response based on check-in/learning/session state | e2e: companion tests verify greeting changes after check-in/study; Phase 3 session/check-in tests (10+) | **Implemented** | Indirect visibility via companion copy; no dedicated "session reward" UI card |
| **8. Smoke / closeout** | Phase 5: 6 smoke chains all PASS; 7 regression areas all PASS | Flutter: 44 widget tests all pass; `flutter analyze` 0 errors | 16 unit + 67 e2e + 44 Flutter = 127 total, all pass | **Implemented** | 0 blockers, 0 major bugs, 4 cleanup issues |
| **9. Persistence** | `DevStorePersistence` in `persistence.ts`; `serialize()`/`hydrate()`/`saveToDisk()`/`loadFromDisk()` in DevStore; 14 save points; atomic write (tmp+rename) | No Flutter changes needed | unit: 5 persistence tests in `persistence.spec.ts` | **Implemented** | File-backed JSON; single-user MVP; state survives restart |
