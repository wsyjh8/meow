import 'package:flutter/material.dart';

/// SettlementPage - 结算
///
/// Handles reward settlement layer.
/// Placeholder for Phase 1 (out of scope).
class SettlementPage extends StatelessWidget {
  const SettlementPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('结算'),
      ),
      body: const Center(
        child: Text('SettlementPage - Phase 1 暂不实现'),
      ),
    );
  }
}
